service {
  hostAddress = "http://localhost:8081"
  apiPath = "/product/"
}

scenario {
  repeatAmount = 5
  threadsAmount = 9
  successRate = 100
}

data {
  requests = ["Fanta", "Coke", "Glow", "Sun", "Bronze",
    "Gerber", "Music", "Summer", "Flow", "Sprinkles", "Cookie", "Orange"]
}